<div style="margin: 20px 40px;">

    <div style="margin:10px;">
        <% if current_user.id == session[:user_id] %>
            <% @logs.each do |log_entry| %>
                <h3><a href="/logs/<%= log_entry.id %>"><%= log_entry.shoot_title %></a></h3>
                <h5><%= log_entry.created_at %></h5>
            <% end %>
        <% end %>
    </div>

</div>